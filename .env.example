# Local Django API for RTK Query (exposed via next.config env.SERVER).
# Use localhost (not 127.0.0.1) so session cookies work with the site on localhost:3000.
# Production: SERVER=https://server.orbeetal.com  (see .env.production)
SERVER=http://localhost:8000
